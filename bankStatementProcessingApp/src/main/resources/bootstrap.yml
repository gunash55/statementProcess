---
spring:
  application:
    name: customerStatementValidatorApp
  cloud:
    config:
      discovery:
        enabled: false

eureka:
  instance:
    nonSecurePort: 8484
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/